import React from 'react';
import styled from 'styled-components';

export const MonButton = () => {
    return (
        <Button>TOTO</Button>
    )
};

const Button = styled.button`
  background-color: #343434;
  color: white;
`;
